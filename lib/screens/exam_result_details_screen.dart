import 'dart:io';

import 'package:flutter/material.dart';

class ExamResultDetailsScreen extends StatelessWidget {
  static const routeName = '/categories/exam/resoult-details';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('szczegóły')),
      
    );
  }
}